09/09/2029
Se corrigió algunos datos de configuración base de Django y se preparó movió la SECRET_KEY a un archivo .env.
Se corrigió el error de ALLOWED_HOSTS (AALLOWED_HOSTS anteriormente) y se dejó la configuración del proyecto más estable para desarrollo local.
Se creo la autenticación en la app accounts para poder hacer login, register principal.
Se mejoró la estructura del proyecto para que el home y las páginas principales queden separadas de la autenticación.
Se creó un sistema de registro personalizado con "Huella ID", "nombre de usuario", "nombre" y contraseñas.
Se eliminó (o se suprimió ocultandolo...) la lógica de django de errores y se mejoró el diseño del formulario para que parezca más a Instagram.
Se separó el CSS de autenticación del CSS global y se ajustó la vista de login/register para que mantenga una misma identidad visual.
Se creó la base del modelo de perfil del usuario con relación 1:1 hacia User y campos como paw_id, username y preparación para avatar/bio para luego.
Se añadió Pillow en requirements.txt para soportar ImageField del avatar y se validó el proyecto con python manage.py check para asegurarme de que por lo menos luego de la migración, todo vaya bien.
Se dejó la base lista para seguir con home -> perfil -> posts -> historias -> feed -> reels, y lo que siga adelante.
